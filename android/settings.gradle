rootProject.name = 'GigHala'

// Capacitor generates this file during `npx cap sync android`
// It includes all plugin subprojects automatically
apply from: 'capacitor.settings.gradle'

include ':app'
