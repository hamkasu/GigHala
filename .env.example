# GigHala Environment Configuration
# Copy this file to .env and update with your actual values

# Flask Configuration
SECRET_KEY=your-secret-key-here-use-a-strong-random-value
FLASK_ENV=production
FLASK_DEBUG=False
PORT=5000

# Database Configuration (Principle of Least Privilege)
# App connection - LIMITED privileges (SELECT, INSERT, UPDATE, DELETE only)
# Use this for production runtime
DATABASE_URL=postgresql://gighala_app:app_password@host:port/database_name

# Admin connection - FULL privileges (CREATE, DROP, ALTER, etc.)
# Use this ONLY for migrations and maintenance - NEVER in production code
DATABASE_ADMIN_URL=postgresql://gighala_admin:admin_password@host:port/database_name

# Security Configuration
# CORS - Comma-separated list of allowed origins (use * for development only)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# OAuth Configuration (Social Login)
# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Apple Sign In
APPLE_CLIENT_ID=your-apple-client-id-here
APPLE_TEAM_ID=your-apple-team-id-here
APPLE_KEY_ID=your-apple-key-id-here
APPLE_PRIVATE_KEY_PATH=path/to/your/apple/private/key.p8

# Microsoft OAuth
MICROSOFT_CLIENT_ID=your-microsoft-client-id-here
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret-here

# Email Service (Brevo - formerly Sendinblue)
BREVO_API_KEY=your-brevo-api-key-here
BREVO_FROM_EMAIL=noreply@yourdomain.com
BREVO_FROM_NAME=GigHala

# SMS Service (Twilio)
TWILIO_ACCOUNT_SID=your-twilio-account-sid-here
TWILIO_AUTH_TOKEN=your-twilio-auth-token-here
TWILIO_PHONE_NUMBER=your-twilio-phone-number-here

# Payment Processing (Stripe)
STRIPE_SECRET_KEY=your-stripe-secret-key-here
STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key-here
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret-here

# Application Configuration
BASE_URL=https://yourdomain.com
TIMEZONE=Asia/Kuala_Lumpur

# Security Notes:
# 1. Always use a strong SECRET_KEY in production (minimum 32 random characters)
# 2. Set FLASK_ENV=production in production environments
# 3. Never set FLASK_DEBUG=True in production
# 4. Restrict ALLOWED_ORIGINS to your actual domain(s) in production
# 5. Use HTTPS in production for secure session cookies
# 6. Database Security (Principle of Least Privilege):
#    - DATABASE_URL uses gighala_app role (limited privileges)
#    - DATABASE_ADMIN_URL uses gighala_admin role (full privileges)
#    - Use different passwords for each database role
#    - NEVER use DATABASE_ADMIN_URL in production application code
#    - Only use DATABASE_ADMIN_URL for migrations and maintenance
# 7. Keep OAuth credentials secure and never commit them to version control
# 8. See DATABASE_SECURITY_SETUP.md for database role configuration
