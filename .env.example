# GigHala Environment Configuration
# Copy this file to .env and update with your actual values

# Flask Configuration
SECRET_KEY=your-secret-key-here-use-a-strong-random-value
FLASK_ENV=production
FLASK_DEBUG=False
PORT=5000

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database_name

# Security Configuration
# CORS - Comma-separated list of allowed origins (use * for development only)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# OAuth Configuration (Social Login)
# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Apple Sign In
APPLE_CLIENT_ID=your-apple-client-id-here
APPLE_TEAM_ID=your-apple-team-id-here
APPLE_KEY_ID=your-apple-key-id-here
APPLE_PRIVATE_KEY_PATH=path/to/your/apple/private/key.p8

# Microsoft OAuth
MICROSOFT_CLIENT_ID=your-microsoft-client-id-here
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret-here

# Security Notes:
# 1. Always use a strong SECRET_KEY in production (minimum 32 random characters)
# 2. Set FLASK_ENV=production in production environments
# 3. Never set FLASK_DEBUG=True in production
# 4. Restrict ALLOWED_ORIGINS to your actual domain(s) in production
# 5. Use HTTPS in production for secure session cookies
# 6. Ensure DATABASE_URL uses strong credentials
# 7. Keep OAuth credentials secure and never commit them to version control
